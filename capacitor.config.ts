import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.plugindemo',
  appName: 'PluginDemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
